<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/ActionBar</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppPrecarga" parent="android:Theme.Holo.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- ActionBar Background Color AND TEXT COLOR -->
    <style name="ActionBar" parent="android:style/Widget.Holo.ActionBar">
        <item name="android:background">#4099FF</item>
        <item name="android:titleTextStyle">@style/ActionBarTitleText</item>
        <item name="android:icon">@drawable/logoactionbar</item>
        <item name="icon">@drawable/logoactionbar</item>
    </style>

    <!-- ActionBar title text -->
    <style name="ActionBarTitleText"  parent="android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>
